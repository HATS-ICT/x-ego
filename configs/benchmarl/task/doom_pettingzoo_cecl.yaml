config_path: "deadly_corridor.cfg"
num_players: 4
host_ip: 127.0.0.1

mode: PLAYER
ticrate: 35
skip: 1
screen_resolution: RES_320X240
window_visible: false
console_enabled: false

timelimit_minutes: null

# Use a different base port for CECL experiments so they can run in
# parallel with other DoomPettingZoo tasks without network conflicts.
base_port: 15029
port_stride: 10

# Let per-env port selection logic still append the concrete "-port" value.
extra_game_args: ""

tile_windows: false
win_x: 80
win_y: 80

frame_stack: 1
max_steps: 1000

wrapper_kwargs: {}



