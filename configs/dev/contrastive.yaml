# Dev overrides for contrastive learning

data:
  batch_size: 4
  num_workers: 0

training:
  max_epochs: 2
  limit_train_batches: 2
  limit_val_batches: 2
  limit_test_batches: 2
  log_every_n_steps: 1

wandb:
  enabled: false
